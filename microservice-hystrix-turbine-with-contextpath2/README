dashboard只是监控一个,turbine对hystrix.stream进行了合并,可以监控集群的情况
turbine访问url:
http://localhost:port/turbine.stream?cluster=server.name(注册的服务的名字)

把上面的url复制到dashboard中就可以监控了.

此工程的设置,当为服务添加了contextpath时,第二种配置turbine的方式